# Prompt: Author ONE glossary term (draft)

> Generated by prompts/glossary/research-next-terms.md on 2026-06-15. **Read prompts/_CONTEXT.md first.**

## Term to author
- **Term:** Crawlability for AI bots
- **Slug:** crawlability-for-ai-bots
- **Cluster:** technical  (one of: ai-search-core | measurement | technical | industrial-ecommerce | roles)
- **Opportunity (editorial, not rendered):** own  (own | contest | reference-only)
- **Aliases:** AI crawlability, AI bot crawlability
- **Angle (our take in one line):** Whether AI crawlers can actually fetch and parse your pages — JS-rendered catalogs are often invisible to non-rendering bots; server-side rendering is the fix. The AI-bot cut of generic crawlability.
- **Who owns the definition today:** Generic "crawlability" is SEO-owned (vol 700/KD 39) and saturated by SEO glossaries; the AI-bot cut is open — scattered across blog posts, no canonical industrial-grounded reference definition yet.
- **Industrial example to use:** A distributor React catalog returns an empty shell to non-rendering crawlers, so its 50,000 SKUs are uncrawlable; SSR exposes the HTML and the SKUs become retrievable.
- **Related terms to link:** ai-crawler, content-chunking-for-retrieval, llms-txt, retrieval-augmented-generation
- **Measured demand (Ahrefs US, 2026-06-15):** crawlability US vol 700, KD 39 — moderate; AI-bot cut is the open lane

## Task
Create ONE glossaryTerm **draft** in Sanity (drafts.glossary-crawlability-for-ai-bots) following the established
structure and the operator voice. Mirror the field shapes in existing terms and the schema
sanity/schemas/glossary-term.ts. Use a Node script with next-sanity createClient + the write
token (pattern: scripts/seed-glossary.mjs). Build portable-text blocks with unique _keys.

**Before writing, verify the facts** for this term against current sources (WebSearch). Definitions
and any stat must be accurate and neutral. If you assert a real-world example, it must be real and
sourced; otherwise write a clearly-illustrative scenario.

### Fields to set
- term, slug (crawlability-for-ai-bots), cluster (technical), opportunity (own),
  aliases, lastReviewed (today, YYYY-MM-DD), seo ({_type:'seo', metaTitle, metaDescription}).
- **shortDefinition** — THE load-bearing field. One neutral, promo-free sentence answering
  "What is Crawlability for AI bots?", <= 60 words, quotable verbatim into an AI answer. This renders first and
  feeds the DefinedTerm JSON-LD. If the term collides with another meaning, disambiguate in the
  first sentence.
- **body** (portable text), in this order:
  1. h2 "Why it matters" + 1 short paragraph **leading with the industrial example** (A distributor React catalog returns an empty shell to non-rendering crawlers, so its 50,000 SKUs are uncrawlable; SSR exposes the HTML and the SKUs become retrievable.).
  2. Optional h3 "X vs Y" or "How to measure it" sub-section if it's a fan-out magnet. Keep tight.
  3. An **"In practice"** h3 + one concrete usage example. If real, end with a source line
     (a portable-text link mark to the URL). If not verifiable, frame it as an illustrative scenario.
- **relatedTerms** — references to ai-crawler, content-chunking-for-retrieval, llms-txt, retrieval-augmented-generation ({_type:'reference', _ref:'glossary-<slug>'};
  add _weak:true if a target isn't published yet).

### Voice
Operator register (see _CONTEXT.md): terse, declarative, "X not Y", anti-marketing, concrete.
No hype, no "in today's fast-paced world." Industrial examples, not generic SaaS ones.

## Definition of done
- Draft drafts.glossary-crawlability-for-ai-bots exists in Sanity (verify with a perspective:'raw' query).
- npx tsc --noEmit clean (ignore pre-existing lib/lead-form/* errors); changed files lint clean.
- Report the shortDefinition and the example (with its source/illustrative tag) in your reply.
- Do NOT publish — leave it as a draft for operator review (publish via
  prompts/glossary/verify-and-publish-terms.md).
