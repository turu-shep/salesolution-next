# Prompt 7 — Niche template (reusable for any future niche)

**UPDATED 2026-07-07** for the post-research frame: the niche now inherits a signed
architecture, a parent vertical spec, the visible-value rules (R1–R9), and two built
reference pages. **Roofing is already queued as the first niche** — home-services spec
§12 banked its three lead numbers ($17,631 Verisk insurance reroof · Invoca 45%/27%
missed-call pair · ServiceTitan 16% same-day follow-up); for roofing, skip straight to
this template with that section pre-answered. The jewelry niche outline
(consumer-jewelry spec §7) is the sell-product reference; dentists is the book-jobs one.

Fill the {PLACEHOLDERS}, then paste below the line into a fresh Claude Code session.
Prefix with `ultracode`. Use when a niche is earned (a real client or real search
demand) per the Phase-6 lazy rule.

---

You are researching the offer and wording for a new Sale Solution niche page:
**Revenue Engine for {NICHE}**, at flat `/revenue-engine/{slug}/` (book-jobs) or under
`/industries/…` (sell-product), beneath the {PARENT PILLAR} pillar. I'm the founder.
Deliverable: a niche spec that a build session can ship from.

**Read first — the inheritance chain (top wins on conflicts):**
`docs/strategy/offer-research/00-offer-architecture.md` (SIGNED — the spine, D1–D12,
§16), `docs/strategy/offer-research/{PARENT}-offer-spec.md` (the parent vertical's
number bank, qualification floors, three-options skin, page-map patterns),
`docs/strategy/offer-research/02-visible-value-pass.md` (rules R1–R9 for the proposal
layer), `docs/strategy/offer-research/03-migration-build-plan.md` + `04-signoff-sheet.md`
(if built — the canonical copy blocks and which claims rows are already signed),
`.agents/product-marketing-context.md` (motion decides guarantee/voice/CTA — the typed
`motion` field, never the breadcrumb), and the reference page for your motion:
`app/(site)/revenue-engine/dentists/page.tsx` (book-jobs) or the jewelry outline
(consumer-jewelry spec §7, sell-product). `01-anchor-ladder.md` is PARKED — ignore
unless its D13 rows appear signed in the sign-off sheet.

**Research (most niches inherit 70% from the parent spec — verify, localize, source
only what's niche-specific):**
1. **Unit economics + the improved condition.** {NICHE}'s tickets, LTV, close/book
   rates, seasonality — start from the parent spec's bank, add only niche-specific rows
   (URL + date, verification verdict per row). Verdict: does the niche clear the
   ≥10x model on the $30K+ floor (parent spec's qualification rule)? State the floor
   (size/revenue/mix) or decline. Then the Weiss value anchor: the niche's valuation
   multiples (parent bank usually has them), the fee as a fraction of the condition.
2. **The calculator preset.** A `WholeFlowLeak` preset row (Bring/Convert/Retain inputs
   + avg ticket) with sourced defaults that land the leak at 5–15x the floor honestly —
   buyer can lower every input and still clear. Show the sourcing map per default, the
   N-payback unit ({roofs|cases|pieces|jobs}), and the do-nothing line.
3. **Their words.** Forums, trade subreddits, association surveys, reviews of their
   incumbent vendors. The wedge in their vocabulary + the 3 objections unique to this
   niche beyond the parent's set.
4. **The install, localized.** What's live by week 2 / day 30 / day 60 (book-jobs
   compresses to 60 per D5; sell-product stays ~90); compliance analogs
   (HIPAA-equivalent, licensing, TCPA exposure); integrations that matter.
5. **The visible-value SOW layer (R1–R9).** The niche's priced ledger (bought-alone
   rows — every row OPENED at its URL, not SEARCH), the two faces with **native**
   defense content (the niche's own erosion — never another vertical's compliance list;
   never CCPA to sub-$26.6M clients), and the motion-correct bridge (Day-91 owner's
   manual on book-jobs; the transfer columns on sell-product).
6. **Legitimate urgency + capacity.** The niche's real calendar (sourced), and the
   capacity line only with my confirmed N.
7. **Demand check.** DataForSEO/Ahrefs volumes for the niche's high-intent patterns;
   target query per the keyword-ownership rules ("Revenue Engine for {niche}" +
   job-verb).

**Wording kit (motion decides voice; kill-list enforced; humanizer pass):** 3 hero
options mapped to the reference page's section arc, the joined leak-math block with
N-payback in {NICHE} units, offer presentation (inherited canonical blocks — floor line,
credit sentence, terms; do NOT re-word them), 5 niche objection rewrites, CTA microcopy
naming the artifact, the not-doing-this close in their units, FAQ additions
(schema-bound).

**Deliverables → `docs/strategy/offer-research/{slug}-niche-spec.md`:** go/no-go with
the qualification floor · offer table (inherited prices, localized contents) · sourced
number bank with proposed claims rows (GATE:HUMAN) · the calculator preset · wording
kit · full page outline mapped to the reference page's sections · the proposal-sheet
skin (localized conditions for the three §16-named options, ledger, defense face) ·
measurement plan (target query, data-cta tags).

**Constraints:** no fabricated proof (PROOF-SLOT until real); inherit canonical prices
and copy blocks — localize contents, never numbers; the motion's commercial block only
(guarantee XOR published bands); no manufactured urgency; every new number needs URL +
date + GATE:HUMAN; the guarantee sentence is untouchable.
