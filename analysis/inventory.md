# <PROJECT> Content Inventory - for de-duplication (compiled by engine:init)

Any NEW article proposal MUST NOT duplicate anything below. Cross-check every
candidate against: (1) published posts, (2) the Linear backlog, (3) any content
plans, (4) existing collections / money pages. A candidate is "net-new" only if no
item below targets the same primary search intent.

> Regenerable. Re-run engine:init step 3 to refresh from the live sitemaps + Linear.

---

## 1. PUBLISHED posts (live on the site) - DO NOT re-propose
<!-- engine:init step 3 fills these slugs from the configured sitemaps. -->
(empty - run engine:init step 3 to populate from sitemaps)

## 2. LINEAR backlog (by status) - already planned, DO NOT re-propose
<!-- engine:init step 3 fills these from integrations.linear.team. -->
(empty - run engine:init step 3 to populate from Linear)

## 3. CONTENT PLANS (spreadsheets / planning docs)
(list any planning docs the team maintains)

## 4. COLLECTIONS / money pages
<!-- engine:init step 3 fills these from the sitemap's collection/category URLs. -->
(empty - run engine:init step 3 to populate)
