# rollup-rosters — findings (2026-08-03 session)

Everything below was re-derived from source this session. Raw parses with full
provenance: `emails/data/raw/rollup-rosters/` (`rosters-2026-08-04.json` — the
UTC date stamp; fetched late 2026-08-03 local — `sunsource-roster-2026-08-03.json`,
`match-2026-08-04.json`). Every origin request rode `lib/fetch.mjs` (≥3s/host,
serial per origin, cached — a re-run makes zero origin requests, honest UA).

**This workstream added zero contacts.** Seated went **2,782 → 2,736**. That is
the deliverable working as designed: −46 roll-up subsidiaries seated as
independents (~1.7% contamination), plus 12 more retagged across three pools.

---

## 1. The three rosters, re-derived

### Singer Industrial (AEA Investors) — first-hand, dated

- **47 operating companies / 42 distinct domains** on the live
  [`/brands`](https://singerindustrial.com/brands) table (read 2026-08-03).
  Validation had matched against 45 names — the denominator question is closed:
  the page publishes 47 (three brands share `rwconnection.com`, two share
  `capitalrubberco.com`, two carry no site of their own).
- **38 press releases, every one dated** via `article:published_time` (archive
  walk to the 404 stop, then one polite fetch per article for its date).
- Releases name acquisitions **not yet on `/brands`**: Conveyor Consulting &
  Rubber (2026-05-04, `ccrconveyor.com`), TS Products, Mattson Distributing,
  Hotsy Industrial Systems, Hotsy Equipment of Colorado & Wyoming, Hotsy of
  Boulder, ACE Conveyor Services. The roster is `/brands` ∪ release names.
- Discovered infrastructure domains: `fcgdivision.com` **308-redirects into**
  `dfpoemdivision.com` — the Dakota Fluid Power OEM Division storefront (the
  `/brands` row that shows "no site"). Both are Singer-owned surface.

### Motion & Control Enterprises (Frontenac) — first-hand, dated

- [`/about/mce-companies`](https://mceautomation.com/about/mce-companies):
  **19 subsidiary domains** (employee-portal and own-store links excluded).
- [`/about/news`](https://mceautomation.com/about/news): **40 dated items, 26 of
  them acquisition announcements, 2017 → 2026-05-21** — Ritter-era deals
  (Primet, Tri-State Hose, Fluid Systems Engineering, 2017) through Tripp
  Electric Motors (2026-05-21). The MCE denominator is closed: validation's 11
  names were a third of the real roster (~30 named companies).
- One announcement is NOT a company acquisition: 2024-10-01 "Three MFCP
  ParkerStore Locations" — three stores bought FROM MFCP; **MFCP itself stays
  independent** and must never be tagged off this headline.

### SunSource (Clayton, Dubilier & Rice) — second-hand, per policy

- `sun-source.com` was tried **once** on `/` and once on the Google-indexed
  `/LLM-Table-of-Contents`: both return HTTP 200 with the **same 167,100-byte
  client-side-rendered app shell** — GTM bootstrap + framework scripts, zero CMS
  content, empty `<title>`. **The 2026-08-03 "empty bodies" mystery is
  resolved: it is a JS-only site, not a block.** Googlebot renders JS; our
  polite fetcher does not, and no headless render was used (workstream rule).
- The roster is therefore assembled from subsidiary pages and deal coverage,
  **tagged per name** in `sunsource-roster-2026-08-03.json`:
  **11 confirmed** (Price Engineering 2018 · The Hope Group 2019-10-31 ·
  Western Integrated Technologies 2024-01 · Amazon Hose & Rubber 2021-10 ·
  United Central 2018 · Carotek 2021 · Stuart Hose & Pipe · Ford-Gelatt 2012 ·
  Perfection Servo · Callahan Weber 2014-08-01 · Ryan Herco 2018), 4
  deal-listed, ~16 family-listed, and **6 adjudicated NEGATIVE** (§4).
- **Never state a SunSource count as complete.** The confirmed list is a floor.
- Evidence-pattern note: SunSource subsidiaries mostly carry **no homepage
  badge**; where on-page evidence exists it is a footer link ("About
  SunSource" — westernintech, stuarthose, perfectionservo) or the company's own
  history/news page (priceeng, thehopegroup, amazonhose, callahanweber).

---

## 2. Send-list exposure — measured, then closed

Validation's "7 + 6 (3 shared)" was **short**. Re-derived against the full
rosters, both keys:

| List | Roll-up rows | Which |
|---|---|---|
| `first-send-200.csv` | **9** | the known 7 + **`rwconnection.com`** (R/W Connection) + **`texasrubbersupply.com`** (Texas Rubber Supply) — both on Singer's live `/brands` and missed by validation |
| `cohort-e-v1.csv` | **5** | `priceeng.com`, `thehopegroup.com`, `westernintech.com` (the shared 3) + **`questenginc.com`** (row "ritter technology" — Quest Engineering, MCE 2022-09-01) + **`ford-gelatt.com`** (Ford-Gelatt, SunSource 2012) |

The dossier expected a 6th cohort-e row. There isn't one: the four remaining
name-grep candidates (`sloanfluid.com`, `jhf.com`, `aberdeendynamics.com`,
`zemarc.com`) were each checked and are **independent** (§4). Validation's 6th
was a false positive of the name grep.

**Also caught: `westernintech.com` sat on the Track 1 HAND-SEND sheet** (a
named T0 contact) — the one path no automated gate blocks. It is off the sheet
now (suppression runs before Track 1 selection).

**Closure.** `emails/data/suppression/rollup-owned-2026-08-03.csv` (11 domains,
each with parent + evidence_url + confirmed_date) + a small fix to
`s7-export.mjs` so suppressed rows land in `routed.csv` with reason
`suppressed` (its own docstring already promised "every excluded row, with its
reason" — previously they were only a manifest count). Verified in both
previews: **suppressed rows in `routed.csv`, in no batch, off the hand-send
sheet**, conservation exact (pilot: 200 = 34 batch + 3 track1 + 154 routed +
9 suppressed). After the S4j retag the full-list export reads seated-v6 and
suppresses 0 — the rows are structurally gone; the suppression file stays as
the backstop for the static cohort files.

---

## 3. Match results (step 3) — both keys, reported separately

15 targets matched (seated, both send lists, first-send-routed, 10 pools + the
identity backlog) against 166 roster entries. Full detail:
`match-2026-08-04.json`.

- **Domain hits (exact apex — trustworthy):** 48 unique seated domains → 44
  positive + 4 negative-adjudicated; pools: ranked-out 5, small-shops 2,
  above-ceiling 2 (incl. `mceautomation.com` itself, mis-labelled
  `above-ceiling` — the §5c Motion/Grainger class), duplicate-sites 10,
  not-a-distributor 2, chains 5 (the SunSource parent, already caught — the
  existing blocklist working as designed).
- **Name-exact with no domain hit:** 2 seated (`airlinehyd.com` → negative;
  `perfectionservo.com` → confirmed positive) plus no-domain pools
  (segment-w: PPV, Power & Pumps, RSA, Hydraulic Specialists;
  usaspending-unmatched: WIT, United Central, GHX, Livingston & Haven, Custom
  Hydraulic, the MCE parent). **No-domain rows were left untouched** — name-only
  and unconfirmable in one line; both pools are parked/no-send anyway.
- **Name-loose (adjudication pile):** every one adjudicated. Real: none the
  domain key had not already caught. False positives killed: "Fluidyne FP" ≠
  Power & Pumps · "General Machinery Supply" (`genmachinesupply.com`) ≠ MCE's
  General Machinery (`generalmachinery.com`) · "Central Hydraulics MN"
  (`central-hydraulicsmn.com`) ≠ Singer's Central Hydraulics (`cenhyd.com`) ·
  `pgntgroup.com` (a holding company) ≠ SunSource's family-listed Paragon
  Technologies · `amazon.ca`/`amazon.com.mx` ≠ Amazon Hose · assorted
  "electric"/"fluid dynamics" fragments. **The prompt's warning held: name
  matching alone would have buried at least four real independents.**

Validation's "~36 seated" was a **floor, as the dossier said**: the re-derived
rosters produce 46 seated movers (the extra ten include rwconnection,
texasrubbersupply, qualityconveyorsolutions, wattssteamstore, smithindustrial,
stewarthunt, callahanweber, perfectionservo, carotek, stuarthose).

---

## 4. Ownership confirms (step 4) — every mover has one line

Evidence classes used, best-first: parent's own dated release · subsidiary's
own site stating the relationship · current presence on the parent's live
`/brands`-equivalent page.

- **Singer movers (31 seated + 7 pool):** every domain is on the live `/brands`
  table read 2026-08-03 (class 3), many with dated releases on top (class 1);
  `ccrconveyor.com` by the parent's 2026-05-04 release (their own site shows no
  badge — the expected pattern); `fcgdivision.com` by 308-redirect into
  Singer's DFP OEM storefront.
- **MCE movers (5 seated + 3 pool + the parent row):** every domain is linked
  from the live companies page and/or named in a dated acquisition
  announcement. **`instrument associates` (Alsip, IL / junk domain `hubs.li`)**
  confirmed by MCE's own announcement text "Founded in the 1940s and based in
  Alsip, IL" (2021-08-31) — the one name+location-keyed mover.
- **SunSource movers (10 seated + 1 pool):** per-name evidence in
  `sunsource-roster-2026-08-03.json` (own-site badge/history/news page or dated
  deal coverage; each URL recorded).
- **DIVESTED: none found.** No evidence of any matched relationship having
  ended. (The confirms lean on live parent pages and current footers — current
  evidence, not just roster history.)
- **UNCONFIRMED → untouched:** every no-domain name-only match (segment-w,
  usaspending), SunSource deal-listed/family-listed names with no row evidence
  (GHX, K+S, Vytl, Dover Hydraulics, Livingston & Haven — noted for the next
  session), and all name-loose noise.

### Adjudicated NEGATIVE — independents, protected from the tag

Recorded in the roster JSON so no future session re-litigates them:

| Domain | Who | Why they looked owned | Verdict |
|---|---|---|---|
| `zemarc.com` | Zemarc Corp, CA | plausible prior | no acquisition evidence anywhere; presents independent since 1976 |
| `jhf.com` | John Henry Foster, St. Louis | name on family lists | independent Ingersoll Rand master distributor |
| `jhfoster.com` | JHFoster, MN | same name, different company | also not one of our three roll-ups |
| `aberdeendynamics.com` | Aberdeen Dynamics, Tulsa | plausible prior | independent — itself an acquirer (Southwest Seal, 2025-08) |
| `sloanfluid.com` | Sloan Fluid Accessories, Nashville | plausible prior | independent since 1969 |
| `airlinehyd.com` | Airline Hydraulics, Bensalem PA | **the association runs backwards** — Airline bought SunSource Technology Services in 2004 | 100% employee-owned (ESOP) since 2005; **stays in first-send as a real prospect** |

### The two mis-seats — resolved

- **`unitedcentral.net`** — SunSource division (their own page: "In 2018, we
  joined forces with SunSource"). Retagged `chain` with the parent. ✔
- **`singerindustrial.com` / "triad bellows design manufacturing"** — the
  s4d "wearing a domain that is not theirs" class. Both halves resolved:
  **Triad Bellows is real and has its own domain (`triadbellows.com`) — but it
  is itself a Singer operating company** (on `/brands`; "celebrates 15 years"
  release 2025-06-27), so there is no independent company to rescue by domain
  correction. The seated row (Singer's corporate domain) retagged `chain`; the
  real `triadbellows.com` row, found in pool-ranked-out, retagged `chain` too.

---

## 5. The fix (step 5) — route 1, applied through the pipeline

**No schema change was needed — no GATE:HUMAN opened.** Disposition `chain`,
`pool-chains`, and the blocklists already existed; everything below is a change
of data or a new stage script in the established lineage.

1. **`CHAIN_DOMAIN_BLOCKLIST` (lib/dedupe.mjs) +77 apexes**, grouped Singer
   (46: parent + brands + ccrconveyor/fcgdivision/dfpoemdivision) · MCE (20:
   parent + ritter1 + companies) · SunSource (11 confirmed subsidiaries;
   parents were already listed). Confirmed-only: no deal-listed/family-listed
   name entered the blocklist. The negatives are named in the comment as
   never-add.
2. **`CHAIN_BLOCKLIST` +2 name entries** — `Singer Industrial`
   ['singer','industrial'] and `Ritter Technology` ['ritter','technology']
   (both-token rule keeps Singer Equipment, the foodservice independent, safe;
   MCE needs no entry — the existing 'motion' lead token catches it).
3. **`s4j-rollup-retag.mjs`** (s4f/s4h/s4i lineage) applies the same
   `chainDomainMatch()` to today's artifacts:

   | File | Before | After | Moved |
   |---|---|---|---|
   | `lists/seated-v5.csv` → `seated-v6.csv` | 2,782 | **2,736** | 46 |
   | `pool-ranked-out-v7` → `v8` | 13,719 | 13,711 | 8 |
   | `pool-small-shops-v7` → `v8` | 2,814 | 2,812 | 2 |
   | `pool-above-ceiling-v8` → `v9` | 112 | 110 | 2 |
   | `pool-chains-v7` → `v8` | 927 | **985** | +58 |

   **Conservation 20,354 in = 19,369 stayed + 985 chains — PASS. Nothing
   vanished.** Field-for-field readback (§5s) on all five outputs: 0 diffs;
   movers change exactly one field (`disposition` → `chain`); a post-condition
   asserts zero chain-domain rows remain in any staying file. Audit:
   `emails/data/_rollup-retag-2026-08-04.json`.
4. **Consumers bumped to seated-v6:** s7-export, s6-verify,
   declaration-review, suppression-bootstrap (+ dashboard stage label 'S4j';
   the dashboard resolves highest-version automatically). 403 lib tests pass;
   `tsc` and eslint clean on every changed file.

**Deliberately untouched** (roll-up rows present but unmailable and with no
replenishment path; the blocklist fixes them at the next regeneration):
pool-duplicate-sites (10), pool-not-a-distributor (2: `rubberworxhose.com`,
`polyflowhose.com`), pool-segment-w / pool-usaspending-unmatched (name-only,
no domains), pool-non-us. The static cohort files (`first-send-200.csv`,
`cohort-e-v1.csv`) were **not rebuilt** — the suppression join covers them at
export time; rebuilding the pilot cohort (backfill from #206) is a separate
decision left open on purpose.

### Open follow-ups for a next session

- `first-send-200.csv` backfill: 9 suppressed rows shrink the pilot to 191
  effective; §5t's backfill precedent (#201–#205) suggests extending to #214.
- SunSource deal-listed names with no confirmed domain: GHX Industrial,
  K+S Services, Vytl Controls, Dover Hydraulics (2026-05-01 — new; watch for
  it appearing in future pulls), Livingston & Haven.
- `rollup-roster-match.mjs` still reads `seated-v5` (the frozen input of this
  session's step 3); point it at the current generation if re-run.

---

## 6. Deep pass (2026-08-04, same night) — the open follow-ups closed

### The five deal-listed SunSource names — all resolved

- **GHX Industrial → CONFIRMED** (`ghxinc.com`, own site: "GHX Industrial is a
  SunSource Company"; 44 locations) — **and it was still SEATED** as
  "ghxinc / GHX Industrial (Gooding Rubber Co)". The step-3 matcher missed it
  because the row's `company` field is the bare join key "ghxinc"; the real
  name lives only in `company_display`. Matcher fixed (tokens now match both
  fields); row retagged by **S4k**. Its email was NeverBounce-valid and the
  row sat in a live batch of the previous full preview — this catch mattered.
- **K+S Services → CONFIRMED** (2021-07-30, `k-and-s.com`; PrivSource/PitchBook
  + advisor tombstone). Row in pool-ranked-out → retagged by S4k.
- **Vytl Controls Group → CONFIRMED** (closed 2026-01-30, SunSource's own PR;
  32 branches / 1,100+ employees). Row in pool-small-shops → S4k. Its four
  divisions verified as Vytl surface and blocklisted: `setpointis.com`,
  `wosupply.com`, `valsource.net` all 301-redirect into
  vytlcontrols.com/company/*; `atcontrols.com` 301s to `a-tcontrols.com`,
  whose portal/social handles are vytlcontrols.
- **Dover Hydraulics → CONFIRMED** (2026-05-01, PitchBook; `doverhydraulics.com`,
  Dover OH). Not in our data; blocklisted for future pulls.
- **Livingston & Haven → NEGATIVE** (no acquisition evidence anywhere; presents
  independent since 1947; its e-commerce arm is `mrostop.com`, which a seated
  row references). Do not tag.

### McCarty Equipment — the discovery that proves the floor

During the first-send backfill verification reads, `mccartyequipment.com`
(Abilene TX, 17 locations, seated at 74/T0) showed a **SunSource link in its
own footer**. Boxwood's 2019-02 Stuart Hose release then named the whole GHX
family: "Stuart Hose & Pipe joins GHX Industrial alongside **National Mine
Service, Gooding Rubber, and McCarty Equipment Company**." McCarty appears on
NO third-party family list — the roster method cannot see subsidiaries the
parent never lists. **Process rule earned: every individual verification read
checks the footer.** Retagged by **S4n** (see the race note below); National
Mine Service and Gooding Rubber recorded (not in our data / already inside the
GHX row).

### The version race — recorded so the next reader isn't confused

At 00:17:26 this workstream's third pass (`s4l-rollup-retag-3.mjs`) wrote a
seated-v8 without McCarty. At **00:18:46 the AD crew's fold-in — independently
also labeled S4l** (`s4l-ad-foldin.mjs`) — wrote ITS seated-v8 (2,774 = v7 +
39 ad_member crossers + re-rank), overwriting the 80-second-old file. Their
base was v7, so **every S4j/S4k retag survived**; only the McCarty move was
clobbered, and their new pools audit clean against the current blocklist
(their stage routed 4 new AD entities to chains itself). **S4n**
(`s4n-rollup-retag-4.mjs`) re-applied the McCarty move on the merged base →
`seated-v9` = **2,773**, `pool-chains-v11` = 993. Lesson for the pack: two
parallel sessions minted the same generation number ~80 seconds apart; a
session should re-check the highest version on disk immediately before
writing, and stage labels need claiming like folder names do.

### First-send-200 rebuilt (§5t precedent) — `s4m-firstsend-backfill.mjs`

The 9 roll-up rows moved to `first-send-200-routed.csv` (now 14 rows, 56-col
header, §5t's 5 originals preserved verbatim) with `verification:
rollup-owned` + evidence notes. **9 replacements entered, every one read
individually** (distributor / US / no parent badge): binkelman.com,
bkindsolutions.com, cbtcompany.com, dupuyoxygen.com, emersonbearing.com,
lettsvankirk.com, mechanicalpower.net, meritpump.com, metpipe.com — scores
74–80 after the concurrent re-rank, all ≥ the cohort's own floor (65).
Skipped, with reasons in the audit (`data/_firstsend-backfill-2026-08-04.json`):
dentechindustrial + mathmec (fabricator-integrators), illinoiselectric
(repair-primary), motorsandcontrol (HTTP 403 — unverifiable), mccartyequipment
(the roll-up above), autopartintl (already routed in §5t). The cohort is 200
individually-verified rows again; the pilot export reads 200 in / 0 suppressed.
The fold-in's 39 unenriched crossers were deliberately not considered — a
cohort seat requires an individual read; next refresh can re-litigate.

### Bookkeeping

- **62 roll-up rows retagged in total** (S4j 58 · S4k 3 · S4n 1 — S4l-retag-3's
  single move was re-applied by S4n after the race).
- **SunSource confirmed roster: 21 entries** (11 from the main pass + GHX, K+S,
  Vytl + 4 divisions, Dover, McCarty, National Mine Service, Gooding Rubber).
  Still a floor — McCarty proves unlisted subsidiaries exist.
- **Sendable: 363** (S4j took 2 valid emails, S4k took 1 — GHX's; the AD
  fold-in added none).
- **Regen impact measured:** 55 roll-up-domain rows sit in `deduped-v7`; the
  blocklist routes all of them to `chain` at the next S2/S3 regeneration.
- **Family-listed names (Air-Draulics, Fauver, Walter Norris, Warden, …):**
  left as recorded in the roster JSON. None appears in our data; chasing
  13 legacy names with no data presence buys nothing today. Confirm on match,
  as the JSON instructs.
- Docs synced to `seated-v9` across emails/README, the list-guide, the pack
  recon note and the dashboard handoff; suppression README documents the
  rollup-owned file; consumers (s7-export, s6-verify, declaration-review,
  suppression-bootstrap) read seated-v9.
