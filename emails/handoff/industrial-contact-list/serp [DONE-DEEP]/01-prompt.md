# serp — reopen check

Your mission: decide whether a wave 4 is justified, and if you run one, run it on the corrected axis rule rather than either of the two rules that were later proved wrong.

## Read first, in order

1. `../00-README.md` — the pack index: the source registry, the company/person/sendable distinction, and the new-source rule.
2. `./00-README.md` — this source's dossier. Three waves, $5.24, never saturated, stopped on qualification throughput.
3. `../../strategy/00-sourcing-strategy.md` §3 Tier-1 item 5 and §3a E5.
4. `../../strategy/01-build-plan.md` §5b, §5g, §5k, §5m — **the query rule is corrected twice; §5k is the current one.** Read all four so you do not re-derive a rule that was already falsified.
5. `../../../research/04-self-identification-play.md`.
6. `../../../data/raw/_acquisition-log-2026-08-01.md` §4 and the two wave entries.

## The check

Reopen for one of two reasons, and say which:

**(a) Replenishment.** D-01 retires zero-engagement contacts weekly, so the seated list drains.

**(b) A new vertical needs quotable declarations** — dealers publishing their own authorizations in their own words.

**If neither holds: report that, and STOP.** Wave 4 was cancelled on economics, not on yield — at 1,150 cumulative queries the rate was still ~5.3 net-new domains/query with no downward trend. The constraint is qualification capacity, and buying more domains does not add any.

## If you do reopen

- **Use §5k's rule: pull the ladder deep first, then prefer the national brand-agnostic axis.** Depth is worth 1.87–2.17×, axis 1.24–1.44×; geographic-deep beats national-standard outright. Do not use §5b's state-scoping (wrong at scale) or §5g's axis-first reading (confounded — it was mostly ladder depth).
- **GATE:HUMAN before any billed run: state the query count, the credit estimate and the hard ceiling.** Calibration: ~5 domains per query at ~$0.007 each; a wave 4 returns roughly 2,000 domains for about $3.
- **Tune the opening ladder rung first.** 26% of wave-3 spend went on failed rungs against DataForSEO's `40101` depth error.
- **Re-run the ICP classifier before treating any domain count as prospects.** Raw precision on wave 3 measured **56% across the whole population and 96% restricted to rank ≥30** (§5m); 81.8% of wave-3 domains sit below the cut. The ranking, not the classifier, is what keeps aggregators out.
- **Never quote a declaration without checking `declaration_is_negated`.** 2.5% publish the inverse sentence, and three reached `seated-v2` before §5u caught them.
- If a page-verification pass runs: **one request per host, never a second.** The last pass recorded 50 refusals (48×403, 2×429) with no retry and no bypass. Keep that posture.

## When this session's work lands

1. Update §5 Registry row in `./00-README.md` (status, counts, est. left) and the STATUS banner.
2. **RENAME THIS FOLDER** to match the new status — `serp [NEW-STATUS]` — that is how the founder reads readiness from the directory listing. Use `IN-PROGRESS` if you stopped before the plan completed.
3. Sync the pack registry table in `../00-README.md` (SSOT rule: row first, table second).
4. Note the change in `../99-hygiene.md` ONLY if it created files to clean.
